<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	
	<uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
		android:networkSecurityConfig="@xml/network_security_config"
		android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiAjudaAndroid">

        <!-- Tela de Login -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Tela de Cadastro -->

        <activity
    android:name=".activities.LoginActivity"
    android:exported="false" />
        <activity
            android:name=".activities.CadastroActivity"
            android:exported="false"/>
			
		<activity android:name=".activities.MainMenuActivity"
		android:exported="false"/>
		
		<activity
    android:name=".activities.ServicoActivity"
    android:exported="false" />

    		<activity
    android:name=".activities.CadastroServicoActivity"
    android:exported="false" />

    		<activity
    android:name=".activities.ServicoActivity"
    android:exported="false" />

    		<activity
    android:name=".activities.OrcamentoActivity"
    android:exported="false" />

        		<activity
    android:name=".activities.CadastroOrcamentoActivity"
    android:exported="false" />

        		<activity
    android:name=".activities.AvaliacaoServicoActivity"
    android:exported="false" />

        		<activity
    android:name=".activities.AtendimentoServicoActivity"
    android:exported="false" />
    
        		<activity
    android:name=".activities.AprovarOrcamentoActivity"
    android:exported="false" />
    
        		<activity
    android:name=".activities.DetalheAtendimentoActivity"
    android:exported="false" />
    

    </application>

</manifest>
